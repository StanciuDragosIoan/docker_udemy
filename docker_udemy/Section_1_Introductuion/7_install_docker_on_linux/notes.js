/*
    install docker

    sudo apt-get update
    apt-transport-https
    ca-certificates
    curl
    software-properties-common

    curl fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -

*/